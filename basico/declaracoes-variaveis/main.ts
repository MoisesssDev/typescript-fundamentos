let nome: string = "João";
let idade: number = 20;
let isAtivo: boolean = true;

let numerosArray: number[] = [1, 2, 3]; // Array
let pessoaFisica: [string, number] = ["João", 20]; // Tupla
enum DiaDaSemana { Segunda, Terça, Quarta, Quinta, Sexta, Sabado, Domingo }; //enum
let dia: DiaDaSemana = DiaDaSemana.Segunda;
let usuario: { nome: string, idade: number } = { nome: "João", idade: 20 }; //objeto
